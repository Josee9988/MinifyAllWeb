/**
 * Summary: interface that will be used in the input select field.
 */
export interface ILanguagesInterface {
  /**
   * The id that references one value.
   */
  value: number;
  /**
   * The 'label' of text that will be displayed as a select option.
   */
  viewValue: string;
}
